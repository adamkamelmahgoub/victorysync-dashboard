# Copy this file to `server/.env` and fill in real values. Do NOT commit real keys to source control.
# Keep this file checked in so teammates know required env variables.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=service_role_key_here
PORT=4000
# MightyCall credentials
MIGHTYCALL_API_KEY=your-mightycall-api-key
MIGHTYCALL_USER_KEY=your-mightycall-user-key
# Base URL should be the API base (no resource path)
MIGHTYCALL_BASE_URL=https://ccapi.mightycall.com/v4
# Copy this file to set real values. Do NOT commit real service keys to source control.
# Replace the values below in your local `server/.env` (which should be listed in .gitignore)

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here
PORT=4000

# MightyCall credentials (single set; do NOT duplicate)
MIGHTYCALL_API_KEY=your-mightycall-api-key
MIGHTYCALL_USER_KEY=your-mightycall-user-key
# Base URL should be the API base; avoid appending resource paths here
MIGHTYCALL_BASE_URL=https://ccapi.mightycall.com/v4
